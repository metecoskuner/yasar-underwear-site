// Quote feature has been removed. Keep a small stub to avoid import errors in case
// some files still import this component. The UI and submission endpoint have
// been removed per the feature removal request.
export default function QuoteModal(): null {
  return null
}
