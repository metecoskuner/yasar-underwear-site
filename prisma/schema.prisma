generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Product {
  id          String   @id @default(cuid())
  title       String
  description String
  imageUrl    String?
  productCode String?  
  images      Json?    @db.Json
  stock       Int?     
  gender      String?
  isActive    Boolean?
  isFeatured  Boolean?
  createdAt   DateTime @default(now())
}

model SiteContent {
  key   String @id
  value Json?
}

model ContactMessage {
  id        String   @id @default(cuid())
  name      String
  email     String
  phone     String?
  message   String
  read      Boolean  @default(false)
  createdAt DateTime @default(now())
}

model HoneypotLog {
  id             String   @id @default(cuid())
  ip             String
  userAgent      String?
  submittedValue String?
  createdAt      DateTime @default(now())
}

model B2BApplication {
  id        String   @id @default(cuid())
  type      String?  // 'wholesale' or 'private-label'
  payload   Json?    // Full form data
  read      Boolean  @default(false)
  createdAt DateTime @default(now())
}

model Quote {
  id        String   @id @default(cuid())
  payload   Json?    // Full form data
  read      Boolean  @default(false)
  createdAt DateTime @default(now())
}
