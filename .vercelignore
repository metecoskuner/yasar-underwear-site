# Ignore large media and storage directories so they are not bundled into Vercel serverless functions
# See: https://vercel.com/docs/ignoring-source-files

# Top-level folders
uploads
media
storage
.env
.env.local
.env.production
.env.*
!.env.example

# Public media
public/uploads
public/media

# Keep the media index (small JSON) so the API can read the list without bundling binaries
!public/media-index.json
